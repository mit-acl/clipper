from setuptools import setup

setup(
    name='clipperpy',
    version='${PROJECT_VERSION}',
    author='Parker Lusk',
    author_email='plusk@mit.edu',
    description='Robust data association framework',
    package_dir={'': '${CMAKE_CURRENT_BINARY_DIR}'},
    packages=['clipperpy'],
    package_data={'': ['*.so']}
)